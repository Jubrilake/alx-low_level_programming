#include "main.h"

/**
 * mul - multiplies both integers
 * @a: first integer
 * @b: second integer
 * Return: multiplication of both integer
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
